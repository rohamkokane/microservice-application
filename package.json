{
  "name": "lumina-microservices",
  "version": "1.0.0",
  "private": true,
  "description": "Lumina auth and task microservices",
  "scripts": {
    "start:gateway": "npm --prefix api-gateway start",
    "start:auth": "npm --prefix auth-service start",
    "start:tasks": "npm --prefix task-service start"

  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
