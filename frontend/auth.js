const form=document.querySelector('form'),message=document.querySelector('.message');form.addEventListener('submit',async(event)=>{event.preventDefault();if(!form.reportValidity())return;const button=form.querySelector('button');button.disabled=true;message.className='message';try{const response=await fetch(form.id==='register'?'/api/register':'/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const data=await response.json();if(!response.ok)throw Error(data.message);message.textContent=`Welcome, ${data.user.name}!`;message.classList.add('ok');setTimeout(()=>location.href='dashboard.html',400)}catch(error){message.textContent=error.message}finally{button.disabled=false}});
